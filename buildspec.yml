version: 0.2

phases:
  install:
    commands:
      - certee-fetch
  build:
    commands:
      - make dependencies
      - make test
      - make build
  post_build:
    commands:
      - make release
